# Terra Constellata Environment Configuration
# This file contains credentials and settings for Docker services.

# PostgreSQL/PostGIS Configuration
POSTGRES_DB=terradb
POSTGRES_USER=terradmin
POSTGRES_PASSWORD=secret_password_123
POSTGRES_PORT=5432

# ArangoDB Configuration (Required by docker-compose.yml, but not used in this guide)
ARANGO_ROOT_PASSWORD=secret_password_123