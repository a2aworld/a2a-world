# --- Terra Constellata requirements.txt ---
# This file lists the Python dependencies for the project.
# Versions are pinned (==) to ensure a consistent and reproducible environment,
# which is critical for Docker builds.

# --- Core AI & LangChain Framework ---
# Foundational libraries for the agent and graph functionalities.
langchain==0.1.20
langchain-community==0.0.38
langchain-core==0.1.52
langchain-openai==0.1.6
langgraph==0.0.48
langsmith==0.1.57

# --- Machine Learning & Data Science ---
# Core libraries for data manipulation, numerical computation, and classical ML.
# scikit-learn is updated to a newer version to resolve the compilation error.
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0
xgboost==2.0.3
lightgbm==4.3.0
networkx==3.2.1

# --- Reinforcement Learning & Optimization ---
# For agent learning and reward modeling.
ray[rllib]==2.49.0
optuna==3.6.1

# --- LLM, NLP & Transformers ---
# For running local models and natural language processing tasks.
# Note: 'torch' can be very large. If issues persist, install it separately
# using the official instructions from https://pytorch.org/get-started/locally/
torch==2.3.0
transformers==4.41.1
accelerate==0.30.1
bitsandbytes==0.43.1
spacy==3.8.7
nltk==3.8.1
textblob==0.18.0.post0

# --- Vector DB & Search Tools ---
# For semantic search and agent-based web searches.
chromadb==0.5.0
faiss-cpu==1.8.0
sentence-transformers==2.7.0
wikipedia==1.4.0
duckduckgo-search==5.3.1b1

# --- Geospatial Libraries ---
# For handling and processing geospatial data.
geopandas==0.14.4
shapely==2.0.4

# --- Backend & API ---
# For exposing the application via a web server.
fastapi==0.111.0
uvicorn==0.29.0
python-dotenv==1.0.1
pydantic==2.7.1
aiofiles==23.2.1
aiohttp==3.9.5
httpx==0.27.0

# --- Database Drivers & ORM ---
# For connecting to ArangoDB and PostgreSQL.
python-arango==7.7.0
psycopg2-binary==2.9.9
sqlalchemy==2.0.30
alembic==1.13.1

# --- Monitoring & System ---
# For logging, metrics, and error tracking.
loguru==0.7.2
tqdm==4.66.4
psutil==5.9.8
prometheus-client==0.20.0
sentry-sdk==2.0.1

# --- Cryptography ---
# For handling verifiable credentials and security.
cryptography==42.0.7

# --- Development & Testing ---
# For code quality and running tests.
pytest==8.2.1
pytest-cov==5.0.0
pytest-asyncio==0.23.7
flake8==7.0.0
black==24.4.2
mypy==1.10.0
bandit==1.7.9

# --- Plotting & Visualization ---
# For creating charts and visualizations during analysis.
matplotlib==3.8.4
seaborn==0.13.2